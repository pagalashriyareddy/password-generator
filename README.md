# password-generator
This project is a web-based random password generator built using Python and the Flask framework. It allows users to generate secure, random passwords of a specified length using a combination of letters, numbers, and special characters. The application provides a simple interface while performing secure password generation on the server side.
